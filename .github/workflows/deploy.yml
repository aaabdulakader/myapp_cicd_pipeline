name: CI-CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: Run tests
        run: mvn test

      - name: Build app
        run: mvn clean package -DskipTests

      - name: Verify JAR exists
        run: ls -lah target/

      - name: Clean stale app.jar directory
        uses: appleboy/ssh-action@v1.0.3
        with:
         host: ${{ secrets.EC2_HOST }}
         username: ubuntu
         key: ${{ secrets.EC2_KEY }}
         script: |
          if [ -d /home/ubuntu/app/releases/app.jar ]; then
            echo "Removing stale app.jar directory from previous failed run"
            rm -rf /home/ubuntu/app/releases/app.jar
          fi

      - name: Ensure EC2 directories
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            mkdir -p /home/ubuntu/app/releases
            mkdir -p /home/ubuntu/app/current
            mkdir -p /home/ubuntu/app/backup

      - name: Copy JAR to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          source: "target/myapp-1.0.jar"
          target: "/home/ubuntu/app/releases/"
          overwrite: true
          debug: true

      - name: Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            bash /home/ubuntu/deploy.sh
